add_executable(benchmark_client BenchmarkClient.cpp ./pb/benchmark_service.pb.cc ../../include/gayrpc_option.pb.cc ../../include/meta.pb.cc)
if(WIN32)
  target_link_libraries(benchmark_client ws2_32 brynet)
elseif(UNIX)
  find_package(Threads REQUIRED)
  target_link_libraries(benchmark_client pthread brynet protobuf)
endif()

add_executable(benchmark_server BenchmarkServer.cpp ./pb/benchmark_service.pb.cc ../../include/gayrpc_option.pb.cc ../../include/meta.pb.cc)
if(WIN32)
  target_link_libraries(benchmark_server ws2_32 brynet)
elseif(UNIX)
  find_package(Threads REQUIRED)
  target_link_libraries(benchmark_server pthread brynet protobuf)
endif()
